import type { ConnectivityFixtures } from "../types";

// The tag panel reads `tag/list`, which the demo already mocks in ../../tags.
export const tagsFixtures: ConnectivityFixtures = {
  components: ["tag"],
};
